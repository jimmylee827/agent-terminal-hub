node_modules/
dist/
out/
*.vsix
*.tsbuildinfo
.DS_Store

# Evaluation prompts — local only, not part of the package
testing/

# Local-only scratch: backups, notes, anything you want beside the repo but
# never in it. Nothing here is ever committed or pushed.
_unsynced/
